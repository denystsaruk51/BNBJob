import React, {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  offerContainer: {
    flex : 1,
    flexDirection: 'row',
    height: 280,
    marginLeft: 20,
    marginRight: 20,
    marginTop: 10,
  },
});