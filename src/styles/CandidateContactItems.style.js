import React, {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  contactItemsContainer: {
    flex : 1,
    flexDirection: 'row',
    height: 150,
    margin: 10,
  },
});